import { DataMaskDirective } from './data-mask.directive';

describe('DataMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DataMaskDirective();
    expect(directive).toBeTruthy();
  });
});
